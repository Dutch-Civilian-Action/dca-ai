# Relationship Data skill location

The maintained Claude implementation is packaged at:

`plugins/dca-relationship-data/skills/managing-dca-relationship-data/SKILL.md`

Do not maintain a mirrored `SKILL.md` in this provider directory.

The packaged skill implements the provider-independent:

- `agents/relationship-data-agent.md`
- `workflows/reconcile-relationship-data.md`

Claude Tag, Chat/Cowork, and Claude Code distribution should use the packaged plugin where that runtime supports it.
